import Form from "../components/Form";

export default function HomePage() {

    return (
        <main className="h-screen bg-slate-200 flex items-center justify-center">
            <Form />
        </main>
    )
}
